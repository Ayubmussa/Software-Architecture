# Enable when RabbitMQ is running (e.g. docker compose -f infra/docker-compose.yml up -d)
# Run with: --spring.profiles.active=rabbit
spring.autoconfigure.exclude=
spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
